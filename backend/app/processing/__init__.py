"""
app.processing — Phase 5.1 Background Processing Infrastructure

Package components:
  queue   — Abstract queue interface + in-memory implementation
  service — ProcessingService (business logic)
  worker  — Background asyncio worker
"""
